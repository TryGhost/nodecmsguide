{
  "name": "nodecmsguide",
  "version": "0.0.0",
  "repository": "git@github.com:TryGhost/nodecmsguide.git",
  "author": "Ghost Foundation",
  "license": "MIT",
  "main": "app.js",
  "scripts": {
    "dev": "echo \"Implement me!\"",
    "test": "NODE_ENV=testing mocha './test/**/*.test.js'",
    "lint": "eslint . --ext .js --cache",
    "posttest": "yarn lint",
    "preship": "yarn test",
    "ship": "STATUS=$(git status --porcelain); echo $STATUS; if [ -z \"$STATUS\" ]; then yarn version && git push --follow-tags; fi"
  },
  "files": [
    "app.js",
    "lib"
  ],
  "devDependencies": {
    "eslint": "6.6.0",
    "eslint-plugin-ghost": "0.6.0",
    "mocha": "6.2.2",
    "should": "13.2.3",
    "sinon": "7.5.0"
  },
  "dependencies": {
    "bluebird": "^3.7.1",
    "ghost-ignition": "^3.1.0",
    "lodash": "^4.17.15"
  }
}
